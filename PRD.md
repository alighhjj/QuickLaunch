# macOS 12 应用启动器 PRD（产品需求文档）

**产品名称**：QuickLaunch（暂定）  
**文档版本**：v1.0  
**日期**：2026-09-24  
**状态**：草案  
**目标平台**：macOS 12.3+  
**技术栈**：Tauri + Rust + 前端框架（React/Svelte/Vue 任选）  
**分发方式**：GitHub Actions 云端构建，自用无签名分发  
**作者**：个人开发者  

> 本文基于以下需求编写：macOS 12、Tauri、自用、无 Apple 签名认证、GitHub Actions 构建。若产品名、快捷键、技术栈版本有变化，可对应调整。

---

## 1. 文档概述

### 1.1 目的

本文档定义一款运行在 macOS 12 上的**轻量级应用启动器**的产品需求。该工具用于快速搜索、启动本机应用、文件夹、URL 和自定义命令，提升日常操作效率。

### 1.2 范围

- 仅支持 macOS 12.3 及以上系统。
- 仅面向个人自用，不考虑 App Store 分发、企业部署、多用户权限管理。
- 不包含云同步、账号体系、社交功能。
- 构建与打包由 GitHub Actions 完成，本地无需安装 Xcode。

### 1.3 术语

| 术语 | 说明 |
|---|---|
| 启动器 | 通过快捷键唤起，输入关键词后快速打开目标的工具 |
| 条目 | 可被启动的对象，包括应用、文件夹、URL、Shell 命令 |
| 全局快捷键 | 在任意应用中都能触发的系统级快捷键 |
| Ad-hoc 签名 | 使用 `-` 进行的本地签名，无需 Apple 开发者证书 |
| Universal Binary | 同时包含 Intel 和 Apple Silicon 代码的二进制 |

---

## 2. 产品概述

### 2.1 背景

macOS 自带的 Spotlight 和 Launchpad 能满足基础启动需求，但存在以下不足：

- 无法方便地添加自定义 Shell 命令、项目文件夹、常用 URL。
- 排序和结果权重不可控。
- 无法按个人习惯定制界面与快捷键。
- 自用工具无需复杂签名和商店审核，希望快速迭代。

因此，计划用 Tauri 开发一款**自用、轻量、键盘优先、可高度定制**的启动器。

### 2.2 产品目标

1. 按下全局快捷键后，**100ms 内**唤起搜索窗口。
2. 输入关键词后，**50ms 内**返回匹配结果。
3. 支持启动应用、打开文件夹、打开 URL、执行命令。
4. 支持收藏、最近使用、使用频率排序。
5. 在 macOS 12.3+ 的 Intel 和 Apple Silicon 上均可运行。
6. 通过 GitHub Actions 自动构建 `.dmg` 和 `.app`，无需本地 Xcode。

### 2.3 非目标

- 不做跨平台（Windows/Linux）。
- 不做剪贴板历史、窗口管理、插件市场。
- 不做自动更新（自用可手动下载新版本）。
- 不做 Apple 公证和开发者签名。
- 不做多语言国际化（首版仅中文）。

### 2.4 目标用户

| 用户类型 | 特征 | 核心诉求 |
|---|---|---|
| 个人开发者 | 熟悉终端、常用快捷键 | 快速打开项目、执行命令 |
| 效率工具爱好者 | 喜欢键盘操作 | 替代 Spotlight，定制排序 |
| 自用者 | 不需要分发 | 构建简单、无签名门槛 |

---

## 3. 用户场景与用户故事

### 3.1 典型场景

1. **快速启动应用**：按下 `Option + Space`，输入 `chr`，回车打开 Chrome。
2. **打开项目文件夹**：输入 `proj`，选择“项目目录”，回车在 Finder 中打开。
3. **执行常用命令**：输入 `dev`，选择“启动本地开发服务器”，回车执行。
4. **打开常用网页**：输入 `git`，选择“GitHub”，回车用默认浏览器打开。
5. **查看最近使用**：唤起后不输入，直接显示最近使用的 10 个条目。
6. **开机自启**：登录后自动常驻菜单栏，随时可用。

### 3.2 用户故事

- 作为用户，我希望用全局快捷键随时唤起启动器，而不必离开当前应用。
- 作为用户，我希望输入拼音首字母也能搜到应用，例如 `wx` 搜到“微信”。
- 作为用户，我希望收藏常用条目并置顶，减少搜索成本。
- 作为用户，我希望添加自定义 Shell 命令，并能在执行前确认。
- 作为用户，我希望配置能导入导出，方便换机迁移。
- 作为用户，我希望在 macOS 12 上稳定运行，不因系统版本被拒绝。

---

## 4. 功能需求

### 4.1 功能优先级说明

| 优先级 | 说明 |
|---|---|
| P0 | MVP 必须实现，否则产品不可用 |
| P1 | 重要增强，首版尽量实现 |
| P2 | 后续迭代，锦上添花 |

### 4.2 核心功能列表

#### 4.2.1 启动器窗口（P0）

| 编号 | 功能 | 描述 | 验收标准 |
|---|---|---|---|
| F-01 | 全局快捷键唤起 | 默认 `Option + Space`，可自定义 | 任意应用下按下可显示窗口 |
| F-02 | 窗口隐藏 | 按 `ESC` 或再次按快捷键隐藏 | 隐藏后不占用焦点 |
| F-03 | 无边框透明窗口 | 圆角、半透明背景、居中显示 | 视觉上无系统标题栏 |
| F-04 | 始终置顶 | 窗口显示时位于最前 | 不被其他窗口遮挡 |
| F-05 | 多显示器支持 | 在鼠标所在屏幕居中显示 | 切换屏幕后位置正确 |

#### 4.2.2 搜索与结果（P0）

| 编号 | 功能 | 描述 | 验收标准 |
|---|---|---|---|
| F-06 | 关键词搜索 | 按名称、路径、别名模糊匹配 | 输入后实时刷新结果 |
| F-07 | 拼音搜索 | 支持全拼和首字母，如 `wx` → 微信 | 常用应用可命中 |
| F-08 | 键盘导航 | 上下键选择，回车启动 | 无需鼠标即可完成 |
| F-09 | 结果排序 | 综合收藏、频率、最近使用、匹配度 | 常用结果靠前 |
| F-10 | 空状态 | 无输入时显示最近使用或收藏 | 不显示空白 |
| F-11 | 无结果提示 | 显示“未找到匹配项” | 提示清晰 |

#### 4.2.3 条目管理（P0/P1）

| 编号 | 功能 | 优先级 | 描述 |
|---|---|---|---|
| F-12 | 扫描应用 | P0 | 自动扫描 `/Applications`、`/System/Applications`、`~/Applications` |
| F-13 | 解析应用信息 | P0 | 读取 `Info.plist`，提取名称、Bundle ID、图标 |
| F-14 | 启动应用 | P0 | 使用 `open -a` 或 NSWorkspace 启动 |
| F-15 | 自定义条目 | P1 | 支持添加文件夹、URL、Shell 命令 |
| F-16 | 收藏/置顶 | P1 | 收藏后排序权重最高 |
| F-17 | 最近使用 | P1 | 记录最近 50 条，可清空 |
| F-18 | 使用频率 | P1 | 统计启动次数，参与排序 |
| F-19 | 分组/标签 | P2 | 支持按标签过滤 |
| F-20 | 拖拽排序 | P2 | 手动调整收藏顺序 |

#### 4.2.4 设置（P0/P1）

| 编号 | 功能 | 优先级 | 描述 |
|---|---|---|---|
| F-21 | 快捷键设置 | P0 | 修改全局快捷键，检测冲突 |
| F-22 | 扫描路径管理 | P0 | 添加/删除扫描目录，排除项 |
| F-23 | 开机自启 | P1 | 通过 `tauri-plugin-autostart` 实现 |
| F-24 | 主题 | P1 | 跟随系统、浅色、深色 |
| F-25 | 窗口行为 | P1 | 失去焦点是否隐藏、显示条目数 |
| F-26 | 配置导入导出 | P1 | 导出 JSON，导入后恢复 |
| F-27 | 命令执行确认 | P1 | 执行 Shell 前弹窗确认 |
| F-28 | 日志与诊断 | P2 | 查看运行日志，导出诊断信息 |

#### 4.2.5 系统集成（P1）

| 编号 | 功能 | 描述 |
|---|---|---|
| F-29 | 菜单栏图标 | 常驻菜单栏，点击显示/隐藏 |
| F-30 | 通知 | 命令执行完成或失败时发送系统通知 |
| F-31 | 开机自启 | 登录时自动启动 |
| F-32 | 深色模式 | 跟随系统外观 |

---

## 5. 非功能需求

### 5.1 兼容性

- 支持 macOS 12.3、12.4、12.5、12.6、12.7。
- 支持 Intel x86_64 和 Apple Silicon arm64。
- 输出 Universal Binary。
- 前端构建目标需兼容 macOS 12 的 WKWebView。

### 5.2 性能

| 指标 | 目标 |
|---|---|
| 冷启动时间 | < 1.5s |
| 热唤起时间 | < 100ms |
| 搜索响应 | < 50ms（1000 条目） |
| 内存占用 | < 150MB |
| 安装包大小 | < 20MB |
| CPU 空闲占用 | < 1% |

### 5.3 可靠性

- 配置原子写入，避免崩溃导致配置损坏。
- 应用异常退出后，下次启动能恢复默认配置。
- 扫描失败不影响核心启动功能。
- 全局快捷键注册失败时给出提示。

### 5.4 安全性

- 不联网，不上传任何数据。
- Shell 命令执行前需用户确认。
- 路径规范化，防止命令注入。
- 不申请不必要的系统权限。
- 若需读取用户目录，提示可能需要的磁盘访问权限。

### 5.5 可用性

- 全键盘操作。
- 支持 `ESC` 关闭、`↑↓` 选择、`Enter` 启动、`Cmd + ,` 打开设置。
- 界面文字清晰，图标大小一致。
- 支持 Retina 屏幕。

### 5.6 可维护性

- 模块化：搜索、扫描、存储、UI 分离。
- 日志分级：ERROR、WARN、INFO、DEBUG。
- 版本号遵循 SemVer。
- CI 自动构建，产物可追溯。

---

## 6. 技术方案

### 6.1 总体架构

```text
┌─────────────────────────────────────┐
│           前端 UI (WebView)          │
│  React/Svelte/Vue + TypeScript      │
│  搜索框、结果列表、设置页             │
└───────────────┬─────────────────────┘
                │ Tauri IPC
┌───────────────▼─────────────────────┐
│            Rust 后端                 │
│  扫描器、搜索、存储、快捷键、托盘      │
│  tauri-plugin-*                     │
└───────────────┬─────────────────────┘
                │
┌───────────────▼─────────────────────┐
│         macOS 系统 API               │
│  NSWorkspace、Info.plist、文件系统    │
└─────────────────────────────────────┘
```

### 6.2 技术选型

| 层级 | 选型 | 说明 |
|---|---|---|
| 框架 | Tauri v2 | 轻量、Rust 后端、支持 macOS |
| 前端 | Vite + React/Svelte | 按熟悉度选择 |
| 语言 | TypeScript + Rust | 类型安全 |
| 全局快捷键 | `tauri-plugin-global-shortcut` | 官方插件 |
| 开机自启 | `tauri-plugin-autostart` | 官方插件 |
| 本地存储 | `tauri-plugin-store` 或 SQLite | 配置用 JSON，统计用 SQLite |
| 托盘 | `tauri-plugin-tray` | 菜单栏图标 |
| 通知 | `tauri-plugin-notification` | 系统通知 |
| 图标解析 | Rust 读取 `.icns`，转 PNG | 或使用 NSImage |
| 搜索 | Rust 实现模糊匹配 + 拼音库 | 如 `fuzzy-matcher`、`pinyin` |
| CI/CD | GitHub Actions + `tauri-action` | 云端构建 |

### 6.3 关键实现点

- **窗口配置**：`decorations: false`、`transparent: true`、`alwaysOnTop: true`、`skipTaskbar: true`、`visible: false`。
- **扫描逻辑**：遍历目录，过滤 `.app`，读取 `Contents/Info.plist`，提取 `CFBundleName`、`CFBundleIdentifier`、`CFBundleIconFile`。
- **排序算法**：

  ```text
  score = 收藏权重(1000) + 使用频率 * 10 + 最近使用衰减 + 模糊匹配得分
  ```

- **拼音搜索**：将中文名称转为拼音，支持全拼和首字母。
- **命令执行**：使用 `tauri-plugin-shell`，执行前弹窗确认。
- **配置存储**：`~/Library/Application Support/QuickLaunch/config.json`。
- **使用记录**：SQLite 表 `usage(id, item_id, timestamp, count)`。

### 6.4 CI/CD 设计

- 触发：推送 `main` 或手动 `workflow_dispatch`。
- 运行器：`macos-latest`。
- 步骤：
  1. Checkout
  2. Setup Node.js
  3. Setup Rust，添加 `aarch64-apple-darwin`、`x86_64-apple-darwin`
  4. Rust 缓存
  5. `npm install`
  6. `tauri-action` 构建
  7. 上传 `.dmg` 和 `.app` 为 Artifact
- 环境变量：
  - `MACOSX_DEPLOYMENT_TARGET=12.3`
  - `APPLE_SIGNING_IDENTITY=-`
- 产物路径：
  - `src-tauri/target/universal-apple-darwin/release/bundle/dmg/*.dmg`
  - `src-tauri/target/universal-apple-darwin/release/bundle/macos/*.app`

---

## 7. UI/UX 需求

### 7.1 主窗口

- 尺寸：宽 680px，高 420px，可随结果数量微调。
- 位置：鼠标所在屏幕居中。
- 外观：圆角 12px，半透明背景，轻微阴影。
- 搜索框：顶部，占满宽度，自动聚焦。
- 结果列表：每项高 56px，包含图标、名称、类型、快捷键提示。
- 选中项：高亮背景，圆角。

### 7.2 设置窗口

- 标签页：通用、外观、数据源、快捷键、关于。
- 通用：开机自启、失去焦点隐藏、显示条目数。
- 外观：主题、透明度、图标大小。
- 数据源：扫描路径、排除项、刷新按钮。
- 快捷键：全局快捷键、命令执行确认。
- 关于：版本、GitHub 仓库、检查更新（手动）。

### 7.3 交互流程

```text
按下 Option+Space
      ↓
显示窗口，聚焦搜索框
      ↓
用户输入关键词
      ↓
实时搜索结果
      ↓
上下键选择，回车启动
      ↓
窗口隐藏，执行动作
```

### 7.4 空状态与异常

- 无输入：显示“最近使用”和“收藏”。
- 无结果：显示“未找到匹配项，试试添加自定义条目”。
- 扫描中：显示加载动画。
- 快捷键冲突：设置页红色提示。

---

## 8. 数据模型

### 8.1 配置结构（JSON）

```json
{
  "version": 1,
  "general": {
    "launchAtLogin": true,
    "hideOnBlur": true,
    "maxResults": 20,
    "theme": "system"
  },
  "shortcut": {
    "toggle": "Option+Space"
  },
  "scanPaths": [
    "/Applications",
    "/System/Applications",
    "~/Applications"
  ],
  "excludePatterns": ["*.prefPane"],
  "customItems": [
    {
      "id": "custom-1",
      "type": "folder",
      "name": "项目目录",
      "path": "~/Projects",
      "icon": "folder"
    },
    {
      "id": "custom-2",
      "type": "url",
      "name": "GitHub",
      "url": "https://github.com"
    },
    {
      "id": "custom-3",
      "type": "command",
      "name": "启动开发服务器",
      "command": "npm run dev",
      "cwd": "~/Projects/my-app",
      "confirm": true
    }
  ],
  "favorites": ["app-com.google.Chrome", "custom-1"],
  "pinned": ["app-com.apple.Safari"]
}
```

### 8.2 使用记录（SQLite）

```sql
CREATE TABLE usage (
  id TEXT PRIMARY KEY,
  item_id TEXT NOT NULL,
  count INTEGER DEFAULT 1,
  last_used_at INTEGER NOT NULL
);
```

---

## 9. 验收标准

### 9.1 功能验收

- [ ] 按下 `Option + Space` 能唤起窗口。
- [ ] 输入 `chr` 能找到 Chrome，回车可启动。
- [ ] 输入 `wx` 能找到微信（若已安装）。
- [ ] 能扫描到 `/Applications` 下的应用。
- [ ] 能添加自定义文件夹并打开。
- [ ] 能添加自定义 URL 并用默认浏览器打开。
- [ ] 能添加自定义命令，执行前有确认。
- [ ] 收藏后条目排在前面。
- [ ] 最近使用记录正确。
- [ ] 设置修改后重启应用仍生效。
- [ ] 配置可导出和导入。
- [ ] 菜单栏图标可显示/隐藏窗口。
- [ ] 开机自启生效。

### 9.2 兼容性验收

- [ ] 在 macOS 12.3 Intel 机器上运行正常。
- [ ] 在 macOS 12.6 Apple Silicon 机器上运行正常。
- [ ] Universal Binary 可同时在两种芯片运行。
- [ ] 无签名版本通过右键打开可正常运行。

### 9.3 性能验收

- [ ] 冷启动 < 1.5s。
- [ ] 热唤起 < 100ms。
- [ ] 1000 条目搜索 < 50ms。
- [ ] 内存占用 < 150MB。

### 9.4 CI 验收

- [ ] 推送代码后 GitHub Actions 自动构建。
- [ ] 构建产物包含 `.dmg` 和 `.app`。
- [ ] 下载后可通过 `xattr -cr` 放行运行。

---

## 10. 里程碑计划

| 阶段 | 目标 | 内容 | 预计时间 |
|---|---|---|---|
| M1 MVP | 核心可用 | 全局快捷键、搜索、扫描应用、启动应用、基础设置 | 1-2 周 |
| M2 增强 | 提升效率 | 收藏、最近使用、自定义条目、托盘、开机自启 | 1-2 周 |
| M3 打磨 | 体验优化 | 拼音搜索、主题、导入导出、命令确认、性能优化 | 1 周 |
| M4 稳定 | 自用稳定版 | Bug 修复、兼容性测试、文档完善 | 持续 |

---

## 11. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| macOS 12 WKWebView 兼容性 | 前端页面白屏 | 设置 `MACOSX_DEPLOYMENT_TARGET=12.3`，使用兼容构建目标 |
| 全局快捷键冲突 | 无法唤起 | 设置页检测冲突，允许修改 |
| 无签名被 Gatekeeper 拦截 | 首次打开失败 | 文档说明右键打开或 `xattr -cr` |
| Tauri v1/v2 API 差异 | 插件不兼容 | 明确使用 Tauri v2，锁定版本 |
| 扫描权限不足 | 部分目录无法读取 | 提示用户授予完全磁盘访问权限 |
| 命令执行安全 | 误执行危险命令 | 默认确认，支持白名单 |
| CI 构建时间过长 | 迭代慢 | 启用 Rust 缓存，仅构建必要目标 |
| 内存占用过高 | 影响系统 | 限制结果数量，及时释放图标缓存 |

---

## 12. 开放问题

1. 产品最终名称是什么？
2. 默认全局快捷键是否确定为 `Option + Space`？
3. 前端框架选 React、Svelte 还是 Vue？
4. 是否需要剪贴板历史或窗口切换功能？
5. 是否需要支持多语言？
6. 是否需要自动更新？
7. 自定义命令是否需要支持变量替换，如 `{clipboard}`、`{date}`？

---

## 13. 附录

### 13.1 默认快捷键

| 操作 | 快捷键 |
|---|---|
| 唤起/隐藏 | `Option + Space` |
| 关闭 | `ESC` |
| 选择 | `↑` / `↓` |
| 启动 | `Enter` |
| 打开设置 | `Cmd + ,` |
| 清空输入 | `Cmd + K` |

### 13.2 产物下载后运行步骤

1. 从 GitHub Actions 下载 `macos-dmg`。
2. 打开 `.dmg`，拖入 `/Applications`。
3. 右键点击应用 → 打开 → 仍要打开。
4. 若提示损坏，执行：

   ```bash
   sudo xattr -rd com.apple.quarantine /Applications/QuickLaunch.app
   ```

5. 再次右键打开。

### 13.3 后续可扩展方向

- 剪贴板历史。
- 窗口切换。
- 插件系统。
- 自动更新。
- 多语言。
- 与 Raycast/Alfred 工作流互通。